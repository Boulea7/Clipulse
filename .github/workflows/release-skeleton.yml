name: Release Skeleton

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: Release version without the leading v
        required: true
        type: string

permissions:
  contents: write

jobs:
  release-preflight:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Resolve release version
        id: version
        shell: bash
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            version="${{ inputs.version }}"
          else
            version="${GITHUB_REF_NAME#v}"
          fi

          if [ -z "${version}" ]; then
            echo "Release version is empty" >&2
            exit 1
          fi

          echo "value=${version}" >> "${GITHUB_OUTPUT}"

      - name: Validate requested release version
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.value }}
        shell: bash
        run: |
          case "${RELEASE_VERSION}" in
            '' )
              echo "Missing release version" >&2
              exit 1
              ;;
            *[!0-9A-Za-z.+-]* )
              echo "Unsupported release version: ${RELEASE_VERSION}" >&2
              exit 1
              ;;
          esac

      - name: Verify requested tag exists
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.value }}
        shell: bash
        run: |
          git rev-parse "refs/tags/v${RELEASE_VERSION}" >/dev/null

      - name: Check out requested release tag
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: refs/tags/v${{ steps.version.outputs.value }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.12.0
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Install Node.js dependencies
        run: npm ci

      - name: Install Python dependencies
        run: uv sync --frozen --group dev

      - name: Run release prep chain
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.value }}
        run: npm run check:release:prep

      - name: Bundle stable adapter artifacts
        run: npm run bundle:stable

      - name: Generate stable release asset manifest
        run: node scripts/release-assets.mjs manifest

      - name: Generate stable release checksums
        run: node scripts/release-assets.mjs checksums

      - name: Verify stable release manifest and checksums
        run: npm run check:release-assets:stable

      - name: Export stable release asset paths
        id: release_assets
        shell: bash
        run: |
          node scripts/release-assets.mjs github-output >> "${GITHUB_OUTPUT}"

      - name: Upload stable release artifacts
        uses: actions/upload-artifact@v7
        with:
          name: clipulse-stable-release-${{ steps.version.outputs.value }}
          path: |
            ${{ steps.release_assets.outputs.asset_paths }}
          if-no-files-found: error

      - name: Create or refresh draft GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: v${{ steps.version.outputs.value }}
        shell: bash
        run: |
          release_title="Clipulse ${RELEASE_TAG}"
          manifest_asset="${{ steps.release_assets.outputs.manifest_path }}"
          checksum_asset="${{ steps.release_assets.outputs.checksum_path }}"
          mapfile -t upload_assets < <(printf '%s\n' '${{ steps.release_assets.outputs.asset_paths }}' | sed '/^$/d')
          [ -f "${manifest_asset}" ]
          [ -f "${checksum_asset}" ]

          release_lookup_status=0
          matching_releases="$(
            gh api --paginate "repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
              | jq -r --arg tag "${RELEASE_TAG}" '.[] | select(.tag_name == $tag) | [.id, .draft, .created_at, .html_url] | @tsv'
          )" || release_lookup_status=$?
          if [ "${release_lookup_status}" -ne 0 ]; then
            echo "Failed to list GitHub releases for ${RELEASE_TAG}." >&2
            exit "${release_lookup_status}"
          fi
          matching_release_count="$(printf '%s\n' "${matching_releases}" | sed '/^$/d' | wc -l | tr -d ' ')"
          matching_draft_count="$(printf '%s\n' "${matching_releases}" | awk '$2 == "true" { count++ } END { print count + 0 }')"
          matching_non_draft_count="$(printf '%s\n' "${matching_releases}" | awk '$2 != "" && $2 != "true" { count++ } END { print count + 0 }')"

          if [ "${matching_non_draft_count}" -gt 0 ]; then
            echo "Refusing to replace assets on a non-draft release: ${RELEASE_TAG}" >&2
            exit 1
          fi

          if [ "${matching_draft_count}" -gt 1 ]; then
            echo "Found duplicate draft releases for ${RELEASE_TAG}; delete stale drafts before refreshing assets." >&2
            printf '%s\n' "${matching_releases}" >&2
            echo "Delete stale drafts by release id or in the GitHub UI, then rerun this workflow." >&2
            exit 1
          fi

          if [ "${matching_release_count}" -eq 1 ]; then

            for asset_path in "${upload_assets[@]}"; do
              asset_name="$(basename "${asset_path}")"
              asset_id="$(gh release view "${RELEASE_TAG}" --json assets --jq ".assets[] | select(.name == \"${asset_name}\") | .id")"

              if [ -n "${asset_id}" ]; then
                gh release delete-asset "${RELEASE_TAG}" "${asset_name}" --yes
              fi

              gh release upload "${RELEASE_TAG}" "${asset_path}"
            done

            gh release edit "${RELEASE_TAG}" \
              --draft \
              --title "${release_title}"
          elif [ "${matching_release_count}" -eq 0 ]; then
            gh release create "${RELEASE_TAG}" \
              "${upload_assets[@]}" \
              --draft \
              --verify-tag \
              --generate-notes \
              --title "${release_title}"
          else
            echo "Unexpected release lookup state for ${RELEASE_TAG}: ${matching_release_count} matches." >&2
            exit 1
          fi
