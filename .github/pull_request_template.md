## Summary

- 

## Verification

- 
- Confirm `npm run test` still covers root docs parity through `test/**/*.test.ts`.
- If this PR changes repo-level operator/docs contracts, rerun `npm run check:beta` and mention it below.
- `npm run check:beta` is the repo-level closure for these changes and covers both `npm run smoke:adapters` and `npm run smoke:self-hosted`.
- If you only needed a narrow rerun while iterating on operator/docs wording, note whether `npm run smoke:adapters` was also run before handing the PR over.

## Notes

- 
